spring:
  application:
    name: online-store
  datasource:
    url: jdbc:mysql://localhost:3306/online_store?createDatabaseIfNotExist=true
    username: root
    password: root
  jpa:
    show-sql: true
  jwt:
    secret: ${JWT_SECRET}